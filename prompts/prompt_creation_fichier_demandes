A partir du fichier demande en exemple et des et des méta données en exemple, crée moi un fichier de demandes pour le jeu de données titanic

Quelques instructions :
Une fonction gérer demande va passer sur ces lignes de demandes et va les transformer en objet R. Une fonction gérer demande :
- enregistre ensuite la table demandée dans un fichier excel si le nom de fichier xls est renseigné 
- affiche la table demandée dans un html sinon

Les graphqiues deandés le sont directement dans un html

## Informations générales

1) toujours choisir entre type_output graphique ou table quitte à dupliquer les lignes
2) quand aucun nom_fichier_xls n'est renseigné, je suppose que l'on part sur markdown avec kable
3) Ne pas renseigner de fichier xlsx pour les graphiques
4) il y a autant de fonctions que de variables quant(l'application est parallele), la somme est gérée differemmment par rapport aux autres
5) pareil, autant d'unites que de variables unités possibles -> centieme,dixieme,unite,millier,million

Exemple :
sum-mean et poids-CA veut dire sum(poids) et mean(CA), les varianles de croisement sont lesvariables sur lesquelles sont réalisées les group by, les variablesde crisement relative sont celles sur lesquelles sont demandés des part par rapport au total dans les croisements de var_croisement, elles ne sont utiles que pour la sum.

